---
import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/BlogPost.astro";
const allPosts = Object.values(import.meta.glob("./journal/*.md", { eager: true }));
const pageTitle = "The Red Rocket Journal";
---

<BaseLayout pageTitle={pageTitle}>
  <p>This is where I will post about my journey learning Astro.</p>
  <div class="blog-list-wrap">
    {allPosts.map((post: any) => <BlogPost url={post.url} title={post.frontmatter.title} image={post.frontmatter.image.url} />)}
  </div>
</BaseLayout>

<style>
  .blog-list-wrap {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    grid-template-rows: auto;
    gap: 2rem;
  }
</style>
